# 밸런스 11차 — 도적의 밴드 초과는 어느 레버로도 싸게 못 고친다

10차(`c40ee91`)에서 「추진력」의 0딜 재시전을 걷어내자 도적의 실제 위치가 처음으로 보였고,
**Lv20 최종 보스가 밴드 상한(90%)을 넘었다.** 그때 "곡선 / 튜너 SPD 항 / 추진력 효과 중
무엇으로 되돌릴지"를 열어 뒀다. 이번에 그 셋을 포함해 **여섯 개 레버를 실측**했다.

**결론: 코드는 한 줄도 안 바꿨다.** 보스를 실제로 움직이는 레버는 하나뿐인데(LUC 성장 상한,
−12.0%p), 그 대가가 이미 밴드 아래인 칸에 떨어진다. 그리고 그 판단을 가로막는 더 큰 문제가
측정 중에 드러났다 — **전사의 1v3 승률이 Lv10~25 전 구간에서 0.0%**다.

---

## 1. 얼마나 넘었나 — 유물을 든 실측

`TestFile/final_boss_measure.py`(N=200/셀, 시드 20260917, 유물 0/2/3/4개).
기록: `TestFile/final_boss_measure_relics_after10.out`.

| Lv20 최종 보스 | 유물없음 | 2개 | 3개 | 4개 |
|---|---|---|---|---|
| 전사 balanced | 47.0% | 53.5% | 61.0% | 64.0% |
| 마법사 balanced | 54.5% | 58.0% | 59.0% | 60.0% |
| **도적 balanced** | **90.0%** | **93.5%** | **94.0%** | **95.0%** |
| 도적 reactive | 95.5% | 95.0% | 96.0% | 96.5% |

밴드는 보스 45~90%다. 유물 없이 정확히 상한에 걸치고, **유물을 들면 넘는다.** 3차에서
"보스전의 현실은 유물 2~4개"로 확정했으므로 **넘은 것으로 읽는 게 맞다.**

**대조군이 깨끗하다**: 전사·마법사 32행이 10차 이전(`ee9a3ba`) 기록과 **바이트 단위로 동일**하다.
10차 변경이 도적 전용이라는 것의 두 번째 독립 확인이다(첫 번째는 동일 시드 A/B 480전투).

Lv25는 세 직업 모두 92.5~100%인데 이건 설계다 — 5차가 "Lv25 도달은 5.8%뿐, 승리 보너스로 둔다"로
정했다.

## 2. 도적이 이기는 이유는 화력이 아니라 **압축**이다

Lv20 보스전을 행동별로 분해했다(N=200, balanced).

| | 전사 | 도적 |
|---|---|---|
| 보스에게 넣은 총 피해 | 2364 | 2480 |
| 플레이어 행동 수 | **19.4** | **12.9** |
| 행동당 피해 | 122 | **192** |
| 0딜 행동(버프·포션) | 6.7회 (34%) | 2.4회 (19%) |
| 「종언」 피격/전투 | 0.41 | **0.10** |
| 남은 MP | 7.4% | 27.6% |

**총피해는 거의 같다.** 도적은 같은 피해를 **2/3의 행동**으로 넣고, 그래서 보스의 시그니처
패턴 「종언」을 전사의 1/4밖에 안 맞는다. 전사는 MP가 말라 기본공격이 행동의 37%다(7.2회).

압축의 63%가 스킬 하나다 — **「연속찌르기」 5.50시전 / 1562피해.**

## 3. 진짜 미보정 축은 SPD가 아니라 **LUC**였다

「연속찌르기」는 게임에서 **유일한 `multi_hit`** 스킬이고 **도적 Lv5 단독 해금**이다.
타수가 LUC로 정해진다 — `ai/battle/Skills.py:roll_multi_hit_count`:

```
prob = max(base_prob, min(85, attacker.luc * luc_mult - hit_index * prob_decay))
```

`luc_mult 3` · `prob_decay 20` · `max_hits 4` · `dmg_decay 0.68`. 실제 도적 LUC로 계산한
기대 배율(타별 감쇠 반영):

| Lv | 도적 LUC | 기대 타수 | 기대 배율 |
|---|---|---|---|
| 5 | 13.7 | 1.31 | 1.18 |
| 10 | 19.3 | 1.61 | 1.36 |
| 15 | 27.7 | 2.29 | 1.70 |
| 20 | 38.9 | 3.18 | **2.11** |
| 25 | 52.9 | 3.55 | 2.24 (포화) |

**레벨만으로 이 스킬이 2배가 된다.** 그리고:

- **LUC는 도적만 자란다** — `0.6 + lv//6`. 전사 `lv%2` · 마법사 `lv%4`는 진동만 한다.
- **`PlayerPowerIndex.calc`에는 LUC 항이 아예 없다.** HP비율·MP/스킬·아이템·max(stg,sp)·SPD·
  스킬축만 본다. SPD는 최소한 계단이라도 있지만(≥14에서 +0.15) LUC는 **감시 자체가 없다.**
- 보스는 애초에 튜너를 안 거친다.

10차에서 "미보정 축은 SPD"라고 적었는데 **틀렸다. SPD 계단은 포화했을 뿐 존재하고, LUC는 없다.**

## 4. 여섯 레버 실측 — `TestFile/rogue_band_measure.py`

N=150/셀, 시드 20260923, balanced, 22칸. 기록: `TestFile/rogue_band_measure.out`.

- A 현재
- B 연속찌르기 `dmg_decay` 0.68 → 0.58
- C 연속찌르기 `prob_decay` 20 → 32
- D 연속찌르기 `max_hits` 4 → 3
- E **LUC 성장 상한** `0.6 + min(lv//6, 1)` (Lv12부터 증가분 동결)
- F SPD 성장 상한 `0.9 + min(lv//8, 1)` (Lv16부터 동결)

| Lv20 셀 | A | B | C | D | **E** | F |
|---|---|---|---|---|---|---|
| **최종 보스** | 92.7% | 92.0% | 90.7% | 93.3% | **80.7%** | 90.0% |
| 고블린x2(실전 구성) | 95.3% | 94.7% | 94.0% | 97.3% | 89.3% | 92.7% |
| 고블린x3(실전 구성) | 58.0% | 58.0% | 58.0% | 57.3% | 50.0% | 58.7% |
| Lv25 고블린x3 | 96.7% | 96.7% | 96.7% | 96.7% | 94.0% | **88.7%** |
| **밴드 안 칸 수(22칸)** | 4 | 5 | 4 | 4 | **7** | 5 |

읽은 것:

1. **B/C/D(연속찌르기)는 보스를 못 움직인다** — −0.7 ~ +0.6%p. 피해를 깎으면 AI가 더 자주
   걸고(5.62 → 5.88시전) 전투가 길어질 뿐, 도적의 보스전은 **화력이 아니라 생존이 남아도는**
   싸움이라 승패가 안 바뀐다. **기각.**
   덤으로 C·D는 타수를 깎으므로 **유령의 설계된 대응 수단**(`dodge_penalty_per_extra_hit`,
   다단히트로 회피를 깎는 것)까지 같이 깎는다.
2. **E(LUC 상한)만 보스를 실제로 움직인다** — 92.7 → 80.7%. 밴드 안 칸도 4 → 7로 제일 많다.
3. **F(SPD 상한)는 보스를 −2.7%p로 경계선에 걸칠 뿐**이고 Lv25 다대일만 깎는다(96.7 → 88.7%).
4. 보스 쪽 레버는 **5차가 이미 기각했다**(`game/Enemy_Class.py`의 주석) — HP ×1.30에도
   도적 −3%p인데 전사 −49%p, 「종언」을 3→2행동으로 조여도 도적 −3 / 전사 −34.
   보스를 어렵게 만들면 **도적 말고 다른 직업이 먼저 죽는다.**

### 측정에서 한 번 틀렸고, 고쳤다

처음엔 다대일 칸을 전부 「상」 등급으로 고정했는데, `app/Map.py`는 **3마리 노드에서 「상」을
금지한다**(`NORMAL_GRADE_3` = 하/중). 그 칸이 전 팔 0~2.7%로 바닥에 붙어 레버를 구분하지
못했고, 그 상태로 읽었다면 "모든 레버가 다대일을 죽인다 → 전부 기각"이라는 **반대 결론**이
나올 뻔했다. 실전 등급 풀에서 뽑도록 고쳐 다시 돌린 것이 위 표다.

## 5. E를 채택하지 않은 이유

E가 사는 칸은 튜너를 안 거치는 칸이다. **실전 경로(튜너 포함) 숫자는 훨씬 낮다** —
`montecarlo.py` 스윕(N=120/셀, 실전 스폰 구성 + `STAT_SCALE` + 자동 튜닝):

| 1v3 승률 | Lv1 | Lv5 | Lv10 | Lv15 | Lv20 | Lv25 |
|---|---|---|---|---|---|---|
| **전사** | 85.0% | 3.3% | **0.0%** | **0.0%** | **0.0%** | **0.0%** |
| 마법사 | 70.0% | 94.2% | 91.7% | 85.8% | 74.2% | 59.2% |
| 도적 | 75.8% | 3.3% | 0.8% | 1.7% | 5.8% | 45.8% |

| 1v2 승률 | Lv1 | Lv5 | Lv10 | Lv15 | Lv20 | Lv25 |
|---|---|---|---|---|---|---|
| 전사 | 100.0% | 43.3% | 16.7% | 20.0% | 33.3% | 44.2% |
| 마법사 | 100.0% | 100.0% | 97.5% | 94.2% | 98.3% | 95.8% |
| 도적 | 95.8% | 25.8% | 9.2% | 18.3% | **38.3%** | 40.8% |

세 가지가 동시에 걸린다:

1. **E는 도적의 다대일에서 값을 뺀다.** 그 칸들은 실전 경로에서 이미 1v2 38.3% / 1v3 5.8%로
   밴드 하한(45%) 아래다. 상한을 고치려고 하한을 더 내리는 거래다.
2. **튜너가 이 손해를 못 갚는다.** LUC 항이 없으므로 도적이 약해져도 몬스터가 안 내려간다.
3. **`game/Lv.py` 성장 곡선은 4직업 × 전 구간 × 튜너를 다시 재야 하는 변경**이다. 스킬 수치
   한 개(6·7·10차)와는 비용이 다르다.

그리고 순서가 틀렸다. **normal 노드의 25%가 3마리다**(`app/Map.py`의 `randint(1,20)`:
1v1 55% · 1v2 20% · **1v3 25%**). 전사는 Lv5 이후 그 칸을 **한 판도 못 이긴다.** 일반 전투의
1/4이 한 직업에게 사망 판정인 상태에서 다른 직업의 보스 상한 5%p를 먼저 깎는 건 순서가 아니다.

## 6. 남긴 것

코드 변경 없음. 대신 판단이 재현 가능하도록:

- `TestFile/rogue_band_measure.py` — 여섯 레버 비교(스킬 메타·성장식을 그 프로세스에서만
  덮어써 팔을 만든다). 다대일은 **실전 스폰 구성**(`NORMAL_GRADE_POOL`/`NORMAL_GRADE_3`)과
  `STAT_SCALE × _early_game_multi_scale`을 그대로 쓴다.
  ※ 튜너를 안 거치므로 **절대 승률은 실전보다 높다** — 레버의 방향·크기 비교용이고,
  절대값은 `montecarlo.py`를 봐야 한다.
- `TestFile/rogue_band_measure.out` · `TestFile/final_boss_measure_relics_after10.out` — 기록.
- `TestFile/test_rogue_scaling.py` (23검사) — 유지하기로 한 수치를 고정한다.
  「연속찌르기」 메타 4종, 유일한 multi_hit·도적 단독 해금, 도적만 자라는 LUC/SPD 성장식,
  그리고 **`PlayerPowerIndex`에 LUC 항이 없다는 사실 자체**를 고정한다(버그 신고가 아니라
  알려진 미보정 축이라는 표시 — 넣게 되면 이 테스트를 같이 고치고 전 구간을 재측정하라는 뜻).

## 7. 사용자 결정 대기

1. **도적 Lv20 보스 90~95%를 그대로 둘지.** 그대로 두면 밴드를 넘은 채로 간다.
   고치려면 **E(LUC 성장 상한)**가 유일하게 듣는 레버이고, 값은 이미 측정돼 있다(−12.0%p).
   대가는 위 5절.
2. **다대일 절벽을 먼저 볼지.** 전사 1v3 0.0% · 도적 1v3 5.8%는 도적 상한보다 큰 문제다.
   손댈 자리는 `STAT_SCALE`(0.80) · `NORMAL_GRADE_3` · 3마리 노드 비율(25%) — 전부 확정 수치라
   결정이 필요하다.
3. `PlayerPowerIndex`에 **LUC 항을 넣을지** — 일반 몬스터만 따라오고 보스는 여전히 안 따라온다.
