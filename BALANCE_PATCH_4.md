# 밸런스 4차 — 자동 튜닝 입력 정합성 (측정 후 **보류**)

외부 읽기 전용 검토가 올린 6건 중, **자동 튜닝(`core/Balance_Hook.py` →
`ai/Simulator.py` → `ai/battle/Engine.py`) 입력이 실전과 다르다**는 3건을
구현하고 전후를 측정했다.

**판정: 코드는 맞지만 단독 채택하면 실전 밸런스가 나빠진다 — 보류.**
구현은 `tuner-input-parity` 브랜치(`69c6179`)에 그대로 두고,
`beta-deploy-and-sprites`에는 넣지 않았다. 이유는 D절.

나머지 3건(유물 티켓 복구 · 보스 복기 비교 · RL 로그 선택지)은 전투 계산을
건드리지 않아 `4608fdd`로 바로 반영했다 — 같은 시드 A/B 100전투·3,287 step +
튜너 시뮬 30조합이 변경 전과 **IDENTICAL**(RNG 상태 포함).

## A. 무엇이 어긋나 있었나 (재현 확인)

| # | 위치 | 실제로 일어나던 일 |
|---|---|---|
| 1 | `core/Balance_Hook.py::_player_to_snap()` | `job`·`relics`를 안 넘긴다. 실전 변환(`app/Shared.py`)에는 있다. 재현: 도적 + 사제의 유해 → 튜너 입력은 `job='' / relics=[]`. `job`이 비면 **전사 3회 공격당 회복 · 도적 주사위 · 도적 회피 반격 · 탱커 피격 회복**이 전부 발동하지 않는다 |
| 2 | `ai/battle/Engine.py::run()` | `tick_status_effects()`도 `is_paralyzed()`도 호출하지 않는다. 그런데 상태이상을 **붙이기는 한다**(주사위 출혈 3곳, `apply_element_and_react`의 점화·동상·마비). 결과 — 출혈·점화·균열 **피해 0**, 동상 **영구 SPD −50%**(턴이 안 줄어듦), 마비 **무효** |
| 3 | `BalanceHook.item_list` | 생성 시 리스트에 고정. `gs["items"]`는 갱신되는데 전달 경로가 없어, 레벨업 재튜닝이 **새 게임 때의 포션 구성**으로 목표 승률을 잡는다 |

세 가지 모두 방향이 같다 — **튜너가 실제보다 약한 플레이어로 승률을 재고,
목표 승률을 맞추려고 몬스터를 아래로 깎는다.**

## B. 고친 뒤 튜너 출력이 어떻게 달라지나

`tuner_probe.py` — 직업 4 × 레벨 6 × 몬스터 5 × 난이도 3 = 360 표본.
백그라운드 튜닝을 완료까지 기다려 `_monster_cache`에서 직접 읽고(2초 폴백 회피),
조합마다 같은 시드를 다시 심어 한 번에 하나씩만 제출했다.

튜닝된 몬스터 HP 변화(중앙값):

| 직업 | Lv15 | Lv20 | Lv25 |
|---|---|---|---|
| 전사 | +14.1% | +11.0% | +9.0% |
| 도적 | +17.6% | +20.3% | **+24.7%** |
| 마법사 | 0.0% | 0.0% | 0.0% |
| 탱커 | 0.0% | 0.0% | 0.0% |

마법사가 0%인 이유 — 마법사 패시브는 `Elements.py`에 인라인이고 `job`을 보지
않는다. 탱커가 0%인 이유 — 탱커 패시브(`passive_on_hit_received`)를 **엔진이
아예 호출하지 않는다**. 즉 `job` 누락이 실제로 죽이던 것은 전사·도적 패시브다.

## C. 승률 스윕 (N=120, 직업 4 × 레벨 6 × 전투 6 = 144셀)

| 전투 유형 | before | after | 차이 |
|---|---|---|---|
| 1v1 | 94.6% | 90.1% | **−4.5%p** |
| 1v2 | 62.9% | 58.7% | −4.2%p |
| 1v3 | 32.8% | 33.0% | +0.2%p |
| elite | 67.0% | 62.7% | −4.3%p |
| mid_boss | 62.7% | 63.0% | +0.3%p |
| final_boss | 9.8% | 9.9% | +0.2%p |

보스 두 줄이 **대조군**이다 — 보스는 `Make_MidBoss`/`Make_FinalBoss`로 따로
만들어져 튜너를 타지 않으므로 변하지 않아야 하고, 실제로 ±0.3%p다. 측정이
잡은 변화는 튜닝된 몬스터에서만 나왔다.

직업별로는 전사·도적만 움직였고(1v1 −6.4 / −11.8%p), 마법사는 0.0%p로
B절의 튜너 출력과 일치한다.

**도적 정밀 재측정(N=400)** — 노이즈가 아니다:

| 셀 | before | after |
|---|---|---|
| 도적 Lv20 1v1 | 60.5% | **36.5%** |
| 도적 Lv25 1v1 | 65.0% | **43.2%** |
| 도적 Lv20 elite | 30.5% | 18.5% |
| 도적 Lv25 1v2 | 17.0% | 8.0% |

목표 구간(일반 45~88%) 기준으로 셀 9개가 구간 밖으로 나가고 4개가 들어왔다.
나간 쪽에 **도적 1v1**과 **전사 엘리트(Lv15 57→40%, Lv20 53→39%)**가 있다.

## D. 보류 이유 — 측정 중에 더 큰 갈라짐을 찾았다

"플레이어 모델만 정확해졌다"는 게 문제였다. **튜너가 만든 그 몬스터**를 두
경로에 똑같이 넣고 각각 200전투씩 돌린 결과:

| 직업 Lv20 | 몬스터 | 튜너 엔진 | 실전 세션 | 차이 |
|---|---|---|---|---|
| 전사 | 골렘 normal | 97.5% | **0.0%** | +97.5%p |
| 전사 | 고블린 normal | 99.5% | 98.0% | +1.5%p |
| 도적 | 골렘 normal | 77.5% | **4.5%** | +73.0%p |
| 도적 | 고블린 normal | 54.0% | 10.0% | +44.0%p |
| 마법사 | 골렘·고블린 | 100% | 100% | 0.0%p |

골렘·사제·엘리트의 **상태 기계는 `ai/battle_session/`에만 있다** — 엔진은
범용 폴백으로 싸우므로 "골렘"이라는 이름만 같은 다른 몬스터를 상대한다
(CLAUDE.md가 적어 둔 한계이지만, 크기가 "근사치" 수준이 아니다).

여기서 플레이어 모델만 정확해지면 튜너는 **"이 플레이어는 이 골렘을 97.5%로
이긴다"**고 믿고 골렘을 더 키운다. 실전에서는 0%다. C절의 하락은 바로 이
경로로 생겼다.

## E. 다음 순서 (이 순서를 지켜야 한다)

1. **적 모델을 먼저 맞춘다.** 둘 중 하나 —
   (a) 골렘 3페이즈·사제 부활·엘리트 패턴을 엔진에도 넣는다(두 벌 유지 비용),
   (b) `StatTuner`가 `BattleSimulator`(엔진) 대신 **`BattleSession`**을 쓰게 한다
   — `MultiBattleSimulator`가 이미 세션을 쓰므로 선례가 있다. 비용은 속도
   (튜닝 1회 = 이진탐색 ≤20회 × 300~500전투)이므로 `SIM_N`·`MAX_ITER` 재조정이
   같이 필요하다. **(b)를 권한다** — 한 벌만 유지하면 이 종류의 갈라짐이 구조적으로 사라진다.
2. 그 다음에 `tuner-input-parity`(`69c6179`)를 되살려 **같은 측정을 다시** 돌린다
   (`tuner_probe.py` → 전 구간 스윕 → 도적·전사 정밀 재측정).
3. 그래도 도적 1v1이 구간 아래면, 그때 손대는 것은 **몬스터 배율이 아니라**
   도적 쪽 수치다(브리프 「건드리지 않는 곳 ①」).

## F. 재현

| 항목 | 값 |
|---|---|
| 기준 트리 | `4608fdd` (beta-deploy-and-sprites) |
| 변경 트리 | `69c6179` (tuner-input-parity) |
| 튜너 출력 | `tuner_probe.py`, 시드 `20260919`, 조합마다 재시드 |
| 스윕 | `TestFile/montecarlo.py`, 시드 `20260706`, `N=120`(정밀 재측정 `N=400`), `SESSION_ITEMS=1` |
| 두 경로 비교 | 같은 플레이어 스냅샷·같은 튜닝 몬스터, 셀당 200전투 |
| 회귀 | 전체 스위트 33파일 그린(양쪽 트리), `compileall` 통과, `create_app()` 부팅 |
