# 밸런스 8차 — 브리프 3장 「남은 엘리트 로직 3건」

대상: Combat Content Brief 3장 표가 `수정 남음` / `보강`으로 남겨둔 세 건.
결론부터: **① 고블린 대장 호령 구현 · ② 증식 슬라임 생존 분열 + 경직 구현 · ③ 암살자 퇴각 미구현(이름만 정정).**
②·③은 브리프가 **코드를 잘못 읽은 상태로 쓴 항목**이라, 고치기 전에 전제를 먼저 바로잡았다.

측정 도구: `TestFile/elite_logic_measure.py` (팔 A~E, `SUBJECT`/`N`/`LEVELS`/`JOBS`/`AI_MODE` 환경변수).
출력: `elite_logic_measure_goblin.out` · `_slime.out` · `_assassin.out` (전부 N=100/셀, 시드 20260922, balanced AI).

상대는 **실전 엘리트 노드와 같은 경로**로 만든다 — `hook.get_enemy(difficulty="hard")`로 자동
튜닝된 스탯을 (직업, 레벨, 몬스터)당 **한 번만** 뽑아 모든 팔이 같은 적을 상대하고, 동료가 있는
셀에는 실전과 같이 `ELITE_STAT_SCALE[2] = 0.90`을 적용한다.

> **초반에 잡은 계측 결함 하나.** 처음에는 전투 도중에 `get_enemy()`를 불렀는데, 튜너의
> 백그라운드 시뮬이 **전역 `random` 모듈**을 쓰기 때문에 튜닝이 첫 전투 중에 끝나면 그 전투의
> 난수열만 어긋난다. 실제로 팔 A만 승률이 달라졌다(같은 시드인데 A 100% / B·C·D 83.3%).
> 팔 루프 **전에** `_sim_ready`를 기다려 튜닝을 끝내 두는 것으로 고쳤고, 그 뒤로는 호령이
> 0회인 팔이 A와 셀 단위까지 정확히 일치한다.

---

## ① 고블린 대장 「호령」 — 구현 (효과는 실측상 0)

브리프: *격노 후 3턴마다 호령 — 살아있는 동료 STG +10%(2턴). 지금은 격노가 1회성이라 이후 행동이 일반 몬스터와 같아짐.*

### 팔

| 팔 | 내용 |
|---|---|
| A | 현재 (호령 없음) |
| B | 브리프 문자 그대로 — **격노 후에 세기 시작**해 3행동마다 |
| C | 게이트 제거 — 전투 시작부터 3행동마다 |
| D | B와 같되 호령이 **대장의 행동을 소비** |
| E | 카운터는 전투 시작부터 돌고 **발동만 격노 후** |

### 결과 (36셀 평균, N=100/셀)

| | A | B | C | D | E |
|---|---|---|---|---|---|
| 승률 | **43.5%** | 43.4% | 43.3% | 44.2% | 43.4% |
| 호령/전투 (동료 있는 셀) | — | 0.00~1.41 | 0.00~2.35 | 0.00~1.17 | 0.00~1.92 |

(마법사 셀은 어느 팔이든 거의 0이다 — 전투가 4~10턴에 끝나 대장이 격노까지 가지 못하거나
가자마자 죽는다. 위 범위의 윗값은 전사·도적 셀에서 나온다.)

읽을 것 셋.

1. **대장 단독 셀에서는 어느 팔도 0.00회.** 호령은 동료 버프이고, `_make_elite_encounter`는
   고블린 대장을 `randint(1,2)`로 **절반은 단독**으로 내보낸다. 절반의 전투에서는 아무 일도 안 일어난다.
2. **버프 자체가 승률을 못 움직인다.** C는 B보다 3~10배 자주 터지는데 승률은 오히려 −0.2%p다.
   +10% STG를 2/3턴 동안 **동료 한 마리**에게 주는 것은 적 전체 DPS로 환산하면 약 +2%로,
   N=100의 분해능 아래다. "창이 짧아서 안 터진다"가 아니라 **터져도 크기가 없다.**
3. **D(행동 소비)는 플레이어가 유리해진다** (+0.7%p). 대장이 자기 공격을 포기하고 얻는 것이
   동료 +10%뿐이라 엘리트가 약해진다 — 브리프의 의도와 반대다.

### 채택: E

승률이 A와 −0.1%p인 것은 B·E가 같고, **발동 횟수는 E가 B의 2~5배**다(전사 Lv9 대장+박쥐 0.36 → 0.92,
도적 Lv3 대장+박쥐 0.51 → 1.05). 브리프의 "격노 후"를 지키면서 — 발동은 여전히 격노 뒤에만 열린다 —
격노한 그 행동에서 바로 한 번 터지므로 플레이어가 실제로 **본다**. 3장의 공통 결론이
"엘리트에 필요한 건 새 패턴이 아니라 예고의 가시화"이므로, 같은 값이면 보이는 쪽을 고른다.

구현에서 못 박은 것 셋 (`Elite_Actions._elite_goblin_pre`):

- **대장 자신은 대상이 아니다.** `apply_buff`는 같은 `stat`을 **덮어쓴다**. 분노도 STG 버프
  (+15%, 무기한)라, 자신에게 호령(+10%, 2턴)을 걸면 **격노가 오히려 약해진다.**
- **무료다(행동을 소비하지 않는다).** 전투 함성·분노와 같은 사전 처리 자리이고, D의 측정이
  "소비형은 엘리트를 약하게 만든다"를 보여줬다.
- **동료가 없으면 카운터를 소비하지 않는다.** 단독 대장은 아무 일도 하지 않고, 사제가 동료를
  되살리면 그 다음 행동에서 바로 터진다.

배지는 붙이지 않았다 — `_pattern_badges`는 `en` 하나만 받아 **동료 생존 여부를 알 수 없어서**,
단독 대장에게 영영 안 터지는 "호령 3/3"을 띄우게 된다. 브리프 3장의 배지 목록에도 고블린 대장은 없다.

---

## ② 증식 슬라임 「분열 직후 본체 2턴 행동 불가」 — 전제를 고친 뒤 구현

### 브리프의 전제가 코드와 달랐다

브리프 표의 현재 패턴 칸: *HP 30%에서 2마리 분열.* 실제 코드는 그렇지 않았다 —
`_split_slime`은 사망 훅에서만 불렸고 그 훅은 `target.hp > 0`이면 즉시 반환했다.
**분열은 본체가 죽는 순간 일어났고, 분열 직후 본체는 이미 시체**라 "2턴 행동 불가"를 걸 대상이 없었다.
표의 "HP 30%"는 자식의 HP 비율(`SLIME_SPLIT_HP_RATIO`)을 트리거로 잘못 읽은 것이다.
(브리프가 스스로 세는 "코드를 잘못 읽은 것" 6건에 이어 7번째.)

그래서 고칠 수 있는 형태로 먼저 되돌렸다 — **본체가 살아있는 채 HP 30%를 지날 때 분열**.
그러면 브리프의 "2턴 행동 불가"가 비로소 걸 자리가 생긴다.

### 팔과 결과 (12셀, N=100/셀)

| 팔 | 내용 | 평균 승률 |
|---|---|---|
| A | 현재 — 사망 시 분열 | **96.5%** |
| B | 생존 분열 + 본체 2행동 경직 | 92.2% |
| C | 생존 분열만 (경직 없음) | 86.1% |

밴드가 갈리는 셀만:

| 직업·Lv | A | B | C |
|---|---|---|---|
| 전사 Lv7 | 100.0% | 98.0% | 97.0% |
| 전사 Lv9 | 99.0% | **76.0%** | 39.0% |
| 도적 Lv7 | 86.0% | 70.0% | 59.0% |
| 도적 Lv9 | 73.0% | 63.0% | 41.0% |
| 마법사 전 구간 | 100% | 100% | 100% |

**총 HP 풀은 세 팔이 모두 같다** (본체 100% + 새끼 2×30% = 160%). 달라지는 것은 **행동 수**뿐이다 —
생존 분열이면 본체와 새끼 둘이 **동시에** 움직이므로 같은 HP를 같은 턴에 더 많이 때린다.
경직 2행동이 그 증가분을 되돌려주는 자리이고, B가 A와 C의 딱 사이에 앉는 것이 그 증거다.
평균 턴이 A 23.1 → C 20.0(전사 Lv9)으로 **줄어드는** 것도 같은 이야기다(더 빨리 죽는다).

### 채택: B (= 브리프가 쓴 그대로, 2행동 경직 포함)

- 증식 슬라임은 `_ELITE_SOLO_ONLY`라 동료가 없는 **가장 쉬운 엘리트**다. A에서 평균 96.5%로,
  같은 엘리트 묶음의 다른 몬스터(전사 기준 0~22%)와 비교하면 밴드 밖이다. B는 전사 Lv9를
  99% → 76%로 내려 CLAUDE.md가 적어둔 45~88% 목표 밴드 **안으로** 넣는다.
- C(경직 없음)는 39%까지 떨어져 반대쪽으로 밴드를 벗어난다. 경직은 장식이 아니라 필수다.
- 마법사는 전 구간 100% 그대로 — 이 변경은 마법사의 엘리트 우위를 건드리지 않는다.

### 남긴 폴백 하나

문턱 구간을 **한 방에 건너뛰어 즉사**시키면 예전처럼 사망 시점에 분열한다.
이렇게 두지 않으면 한 방 딜이 큰 플레이어에게만 총 HP 풀이 160% → 100%로 줄어든다.
실측에서 마법사는 레벨에 따라 전투의 **최대 35%**가 이 경로다(`분열` 열 0.65~1.00).

---

## ③ 그림자 암살자 「퇴각 보상 완화」 — 구현하지 않음

브리프: *퇴각이 곧 보상 전체 소멸이면 "잡을 수 있었는데 놓쳤다"가 되어 허무. 퇴각 시 골드는 지급 / 경험치는 절반으로 완화 제안.*

### 퇴각은 코드에 없다

`ASSASSIN_RETREAT_HP_THRESHOLD`(0.30)가 실제로 하는 일은 **「추진력」(SPD +10%, 2턴)** 뿐이다.
암살자는 전장을 떠나지 않으므로 **보상이 소멸하는 상황 자체가 없고**, 따라서 이 항목은
"완화를 어떻게 할지"가 아니라 **"퇴각을 넣을지"**를 먼저 정하는 일이다.

### 넣으면 무슨 일이 생기는가 (실측)

팔 B로 진짜 퇴각(고블린 겁쟁이와 같은 `fled` + `reward_eligible=False`)을 붙여 재봤다.

| 셀 | 퇴각 발생/전투 | **A에서 "뺏겼을" 전투 비율** |
|---|---|---|
| 전사 Lv10~20 암살자 단독 | 0.36~0.88 | **38~92%** |
| 도적 Lv10~20 암살자 단독 | 0.36~0.76 | **82~93%** |
| 마법사 Lv10~20 암살자 단독 | 0.52~0.71 | 54~72% |
| 마법사 Lv10~20 암살자+유령 | 0.76~0.84 | 80~91% |

"뺏겼을 전투"는 **A에서 암살자가 HP 30% 아래로 내려갔고 그 뒤 실제로 처치된** 전투다 —
퇴각이 있었다면 그 처치가 사라졌을 판이다. 승률은 A 71.4% → B 72.5%로 거의 안 움직인다
(암살자가 빠지면 그냥 이긴 것이 되기 때문). 즉 **퇴각은 난이도가 아니라 보상만 깎는다.**

### 결론

엘리트 암살자전 **10번 중 4~9번**에서 엘리트 보상이 날아간다. 골드를 주고 경험치를 반만 깎아도
(그리고 `reward_eligible`이 지금은 bool 하나라 그 "절반"을 위한 새 구조가 필요하다)
브리프가 스스로 "허무하다"고 쓴 그 체감이 대부분의 판에서 그대로 발생한다.
**퇴각을 구현하지 않는다.** 대신 코드가 없는 동작을 광고하지 않도록
`ASSASSIN_RETREAT_HP_THRESHOLD` → `ASSASSIN_SPRINT_HP_THRESHOLD`로 고치고 근거를 주석에 남겼다.

---

## 바뀐 파일

| 파일 | 내용 |
|---|---|
| `ai/battle/EliteKit.py` | `GOBLIN_RALLY_*` 3개 · `SLIME_SPLIT_TRIGGER_RATIO` · `SLIME_SPLIT_STUN_ACTIONS` 추가, 암살자 상수 이름 정정 |
| `ai/battle/Entity.py` | `EntitySnapshot.split_stun` 필드 |
| `ai/battle_session/Elite_Actions.py` | 호령 · `_check_elite_death` → `_check_elite_hp`(살아있을 때의 문턱도 본다) · `_split_slime(alive=)` |
| `ai/battle_session/Enemy_Actions.py` | 경직 중인 적은 행동을 통째로 건너뛴다(버프/디버프 틱은 그대로) |
| `ai/Battlesession.py` | DoT로 **죽지 않고 문턱만** 넘은 경우에도 같은 훅을 부른다 |
| `ai/battle_session/State.py` | 배지 「분열 충격」(남은 경직 행동 수) |
| `TestFile/elite_logic_measure.py` | 신규 측정 도구 |
| `TestFile/test_elite_patterns.py` | 66 → **90** 검사 |

## 검증

- 회귀 스위트 **39파일 전부 그린** · `compileall` 통과 · `create_app()` 부팅.
- `test_elite_patterns.py` 90 통과 / 0 실패 (신규 24: 생존 분열·경직 소비·즉사 폴백·DoT 문턱·
  호령 게이트·호령이 분노를 덮지 않음·일반 고블린은 호령 안 함).
- **동일 시드 A/B 480전투 — 전투별 (승자·턴·HP·MP·메시지 해시)와 최종 RNG 해시까지 IDENTICAL.**
  A/B 드라이버는 등급 팩토리를 직접 부르고 엘리트 플래그를 세우지 않으므로, 이 결과는
  **일반 전투가 이 변경에 전혀 닿지 않았다**는 뜻이다.
- **자동 튜닝은 영향을 받지 않는다.** `ai/Simulator.py`와 `core/Balance_Hook.py`에는
  `elite_leader`/`is_elite`가 한 번도 나오지 않는다 — 튜너는 엘리트 패턴을 시뮬레이션하지
  않으므로, 엘리트 패턴을 바꿔도 튜닝된 몬스터 스탯은 움직이지 않는다.

## 건드리지 않은 것

- `PlayerAI("balanced")` 점수 · `GRADE_MULT` · `_level_curve_mult` · `STAT_SCALE` ·
  `ELITE_STAT_SCALE` · 보스 스탯 · 보상 확률.
- 브리프가 2차 3번에서 "보류"로 뺀 슬라임 점착 · 원소 슬라임 상극 취약 · 암살자 기습.

## 측정하다 보인 것 (이번 범위 밖, 기록만)

엘리트 노드는 **전사·도적에게 사실상 닫혀 있다** — 동료가 붙은 셀은 이 측정 전 구간에서 0~21%이고,
과거 전체 스윕(`mc_balance3_after.json`)도 전사 0~22% / 도적 4~21% / 마법사 35~95%로 같은 모양이다.
리더와 동료가 **둘 다 `difficulty="hard"`로 따로 튜닝된 뒤 0.90만 곱해** 합쳐지기 때문으로 보인다.
이번 변경과는 무관하고, 손대려면 별도 항목이다.
