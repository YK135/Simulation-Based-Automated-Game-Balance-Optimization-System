# 밸런스 12차 — 다대일: 튜너가 1v1만 보고 있었다

11차가 "도적 밴드 초과"를 재다가 **다대일이 더 큰 문제**라는 걸 꺼냈고, 그걸 파는 과정에서
**측정 하네스 전체를 무효화하는 버그**가 먼저 나왔다. 두 개를 순서대로 고쳤다.

결과: **1v2가 0/18 → 11/18칸, 1v3이 1/18 → 13/18칸** 밴드(45~88%) 안으로 들어왔다.

---

## 1. 먼저 나온 것 — 튜너가 "스킬 없는 플레이어"를 상대하고 있었다 (`d614105`)

`core/Balance_Hook._player_to_snap`은 스킬을 **1단**으로 읽었다:

```python
if hasattr(player, 'skill') and player.skill:
    skills = list(player.skill.learned_skills)
```

`ai/battle/Entity.EntitySnapshot.from_player`는 **2단**이다 — 같은 조회를 하고, 없으면
`player.learned_skills`로 폴백한다. `player.skill`이 `None`이고 `learned_skills`만 있는
플레이어는 **실전 전투에는 풀 스킬로, 튜너에는 빈손으로** 들어갔다.

- **실전은 무관했다** — `app/Game.py:100`이 `player.skill`을 세우고 `game/Lv._sync_skill_object`가 맞춰 준다.
- **측정은 전부 걸렸다** — `TestFile/montecarlo.py`의 `build_player`를 포함해, 하네스는
  `create_player_by_job`으로 바로 만들어 `.skill`이 `None`이다.

같은 스냅샷으로 튜너의 시뮬 승률과 실제 `BattleSession` 승률을 나란히 재면:

| Lv20 hard | 튜너 시뮬 | 실전 | 격차 |
|---|---|---|---|
| 마법사 | **0.0%** | **100.0%** | +100.0%p |
| 전사 | 38.4% | 99.3% | +60.9%p |
| 도적 | 43.6% | 83.3% | +39.7%p |

마법사는 화력이 전부 스킬이라 시뮬이 "전패"로 읽었고, 튜너는 몬스터를 바닥까지 깎았다 —
**난이도 하/중/상이 563/563/563 HP로 완전히 같아질 만큼**이다. 11차 표의
"마법사만 다대일이 멀쩡하다"는 전부 이 가짜 비대칭이었다.

고친 자리는 개별 스크립트가 아니라 **변환기**다. 회귀는 `TestFile/test_tuner_input_parity.py`
(30검사) — 4차에 같은 함수에서 빠졌던 `job`/`relics`까지 묶어 **모양 전체**를 지킨다.
이 두 변환기가 어긋난 게 두 번째다.

**무효화된 기록**: `mc_patch10_*.json` · `rogue_sprint_sweep.out` · BALANCE_PATCH_10·11의
절대 승률. (10차의 before/after **델타는 유효**하다 — 양쪽을 같은 입력으로 쟀고 바뀐 변수는
「추진력」 지속뿐이다. 두 문서에 경고 상자를 붙였다.)

## 2. 고치고 나니 보인 진짜 문제 — 범주 오류

`TestFile/multi_enemy_tuner_gap.out` (N=150, 실전 스폰 규칙 그대로):

| 튜너 ON | Lv10 | Lv15 | Lv20 |
|---|---|---|---|
| 1v1 전사 | 46.7% | 53.3% | 62.0% |
| 1v1 마법사 | 58.0% | 50.7% | 56.7% |
| 1v1 도적 | 56.0% | 64.7% | 57.3% |
| **1v2 / 1v3 전 직업** | **0.0 ~ 1.3%** | | |

**1v1은 완벽하다.** 아홉 칸이 46.7~64.7%로 목표에 정확히 들어온다. 그런데 2마리부터 전원 0%고,
패배 시 **적 HP가 63~93% 남는다.** 접전이 아니라 계산이 성립하지 않는 것이다.

원인은 상수가 아니라 **층위가 어긋난 것**이다:

1. `get_enemy()`는 **1v1이 약 55%가 되도록** 몬스터 하나를 맞춘다 — 칼날 위에 세운다.
2. `_make_enemies`는 그 몬스터를 **2~3마리** 뽑는다.
3. 보정은 평평한 `STAT_SCALE` **0.90 / 0.80**뿐이다.

턴제에서 n마리는 받는 피해도, 깎아야 할 HP도 n배다. 1v1에서 간신히 이기는 상대를 둘 붙이면
뒤집히는 게 당연하다.

## 3. 고른 길 — 다대일은 튜너를 우회한다 (사용자 결정)

| | 재본 것 | 결과 |
|---|---|---|
| ⓐ `STAT_SCALE` 재조정 | 튜닝 몬스터에 0.80→0.15 스윕 | **계단이다.** 전사 Lv15 3마리가 0.45→5.0%, 0.35→95.0%. 중간이 없다 |
| ⓑ 그룹 튜닝 되살리기 | `get_encounter()`는 이미 구현돼 있다 | 3차의 이진탐색 미수렴은 **이 버그 탓이 아니었다** — 고친 뒤에도 계단이 그대로다 |
| **ⓒ 다대일 튜너 우회** | 등급 팩토리로 바꿔 측정 | **절벽이 곡선이 된다.** 채택 |

ⓒ를 고른 이유는 한 줄로 말할 수 있다 — **튜닝된 몬스터는 "1v1에서 간신히 이기는" 상대라
둘이 되면 즉시 뒤집히지만, 등급 몬스터는 여유가 있어 마릿수에 완만하게 반응한다.**
계단이 곡선이 되니 비로소 숫자로 맞출 수 있다.

**보스가 이미 같은 이유로 튜너를 우회한다**(`Make_MidBoss`/`Make_FinalBoss`는 `_apply_grade`조차
거치지 않는다). 다대일을 같은 부류로 옮긴 것이다.

## 4. 그리고 배율의 방향이 틀려 있었다

우회만 하고 재니 이번엔 **1v2 0/18 · 1v3 1/18로 너무 쉬웠다**(90~100%). `STAT_SCALE`의
0.90/0.80이 **"할인"이라는 전제가 우회와 함께 깨졌기** 때문이다 — 등급 몬스터는 1v1 승률이
이미 100% 수준이라, 약한 상대를 더 깎아 주고 있었다. 필요한 건 할인이 아니라 **가산**이다.

실전 경로 그대로 배율을 스윕했다(`TestFile/multi_scale_measure.out`, N=60/셀). 3마리는
세 직업이 **동시에** 밴드에 드는 값이 깨끗하게 나왔다:

| 3마리 | Lv5 | Lv10 | Lv15 | Lv20 |
|---|---|---|---|---|
| **배율** | **1.6** | **1.4** | **1.2** | **1.0** |
| 전사 | 85.0% | 73.3% | 58.3% | 68.3% |
| 마법사 | 76.7% | 61.7% | 61.7% | 71.7% |
| 도적 | 81.7% | 55.0% | 71.7% | 85.0% |

레벨이 오를수록 가산이 줄어드는 건 `_level_curve_mult()`가 이미 레벨당 +6%씩 몬스터를
올려 주기 때문이다(두 배율이 곱해진다). 2마리는 1.9 → 1.6 → 1.5 → 1.4 → 1.3이다 —
3마리보다 **큰** 가산을 원하는 건 `NORMAL_GRADE_3`가 「상」을 금지해서(하45/중55) 3마리
노드가 애초에 약한 등급만 뽑기 때문이다.

## 5. 전 구간 검증 (N=200/셀, 실전 경로)

| 유형 | 우회 전 | 우회만 | **우회 + 가산** |
|---|---|---|---|
| 1v1 | — | 15/18 | **15/18** |
| **1v2** | **0/18** | 0/18 | **11/18** |
| **1v3** | **1/18** | 1/18 | **13/18** |

밴드 밖으로 남은 것:
- **Lv1 전 칸 95~100%** — 튜토리얼 구간, 기존 설계 여유.
- 전사 1v2 Lv5/Lv10이 89.5 / 90.0%로 상한을 살짝 넘는다.
- **도적 1v2 Lv20·25 (91.5 / 92.5%) · 1v3 Lv25 (97.5%)** — 아래 참고.

## 6. 남긴 것 / 열린 항목

- **엘리트 3/18 (20~37%)** — 이제 게임에서 가장 나쁜 칸이다. 엘리트는 **여전히 튜닝된
  hard 몬스터 + `ELITE_STAT_SCALE` 할인**이라 2절의 범주 오류가 그대로 남아 있고, 게다가
  패턴까지 얹혀 있다. 엘리트는 계속 범위 밖으로 합의돼 온 자리라 **조용히 바꾸지 않았다.**
  다음 결정 대상.
- **도적 1v3이 배율에 반응하지 않는다** — Lv25에서 배율 1.0/1.2/1.4/1.6에 96.7/96.7/90.0/91.7%로
  평평하다. 같은 칸에서 전사는 63.3 → 1.7%로 떨어진다. 몬스터를 60% 세게 만들어도 승률이
  안 움직이는 건 정상이 아니다 — 원인 미규명.
- `get_encounter()`(그룹 튜닝)는 구현·테스트된 채로 남긴다. 연결하려면 계단형 목적함수를
  견디는 분산 감소 설계가 먼저다.

## 7. 코드 변경 요약

- `core/Balance_Hook.py` — `_player_to_snap` 2단 조회(1절) · `make_graded_enemy()` 신설
  (`_make_fallback`은 이걸 「중」으로 부르는 래퍼가 됐고, 난이도 라벨도 등급에서 따라온다) ·
  `prewarm()` 신설.
- `app/Map.py` — `_make_enemies`가 n≥2에서 등급 팩토리 사용 + 예열 · `_multi_stat_scale()`
  신설 · `_apply_stat_scale`의 `scale >= 1.0` 조기 반환 제거(안 지우면 가산이 통째로 무시된다) ·
  `_early_game_multi_scale`은 엘리트 전용으로 축소.
- `TestFile/montecarlo.py` — 같은 규칙으로 동기화(안 하면 스윕이 실전과 다른 걸 잰다).
- 테스트: `test_tuner_input_parity.py`(30) · `test_multi_scale.py`(32).
