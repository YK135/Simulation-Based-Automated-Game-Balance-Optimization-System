# 밸런스 6차 — 흡혈 버프·약점 표식 수지 재조정 (Combat Content Brief 후속 ①)

브리프 11-1 후속 ①: "「피의 격노」·「피의 맹세」·「약점 표식」은 문서 수치 그대로면 수지가 안 맞는다.
**지속 규칙 또는 비용 중 하나를 고르고** 전후를 측정하라."

결론부터: **지속을 골랐다.** 세 스킬 모두 3턴 → 8턴. 비용(HP 15% / HP 20% / MP 11)과
효과 크기(흡혈 25% / 40% / 받는 피해 +8%)와 흡혈 상한(타격 4% · 시전 12%)은 **한 숫자도 바꾸지 않았다.**
비용을 깎는 쪽은 측정에서 **오히려 나빠졌다** — 아래 2장.

측정 스크립트: `TestFile/vamp_mark_measure.py` · 결과 원문: `TestFile/vamp_mark_measure.out`
회귀 테스트: `TestFile/test_vamp_mark_tuning.py` (38개 검사)

---

## 1. 먼저 알아야 할 것 — 이 세 스킬은 기본 AI가 아예 고르지 않는다

`balanced`의 버프·디버프 선택은 `_best_buff_skill(stat="stg"/"arm"/"spd")` ·
`_best_debuff_skill(stat="spd"/"stg")`로 **stat을 지정해서** 후보를 찾는다. 세 스킬의
stat은 `lifesteal` / `lifesteal_oath` / `vulnerable`이라 그 목록에 아예 들어오지 않는다.

그래서:

- **`montecarlo.py` 전 구간 스윕(balanced)으로는 이 세 스킬의 변화를 측정할 수 없다** — 무엇을 바꿔도 0.0%p다.
  브리프 후속 ①이 "montecarlo.py 전 구간 스윕으로 전후 비교"라고 적어 둔 것은 이 세 스킬에는 성립하지 않는다.
  스윕은 **대조군**(다른 게 안 움직였다는 증명)으로만 쓴다.
- 이 스킬을 실제로 쓰는 주체는 **사람**이고, 그 사람을 모델링하는 것은 측정용 AI(`reactive`)뿐이다.
  그래서 전부 `reactive`로 측정했다.
- 따라서 이번 변경은 **자동 밸런싱(StatTuner) 기준을 건드리지 않는다** — 동일 시드 A/B 480전투 IDENTICAL(5장).

## 2. 측정 — 묶인 비용은 HP가 아니라 「행동」이었다

같은 플레이어를 다섯 갈래로 나눠 같은 시드로 돌렸다(N=200/셀, `reactive`, 등급 팩토리 직접 생성).
`A` 스킬 보유 + AI 판단대로 · `B` 그 스킬만 제거 · `C` 조건만 맞으면 무조건 사용 ·
`D` 2택 1의 짝으로 교체 · `E` 첫 기회에 1회만 쓰고 목록에서 제거.

### 2-1. 1회당 수지는 어디서나 0.3~0.5배였다

`E`(1회 시전)에서 피격 장부의 `via="cost"` / `via="lifesteal"`를 그대로 집계한 값:

| 스킬 | 상대 | 지불 | 회수 | 수지 |
|---|---|---|---|---|
| 피의 격노 Lv20 | 골렘상 | 261 | 95 | **0.36×** |
| 피의 격노 Lv20 | 최종 보스 | 245 | 102 | **0.41×** |
| 피의 격노 Lv25 | 골렘상 | 342 | 106 | **0.31×** |
| 피의 맹세 Lv25 | 최종 보스 | 448 | 200 | **0.45×** |
| 피의 맹세 Lv25 | 중간 보스 | 456 | 148 | **0.32×** |

브리프가 적은 "상한에 묶여서"는 **원인이 아니다.** Lv20~25에서 타격 상한(maxHP 4%)·시전 상한(12%)은
거의 걸리지 않는다 — 실제 회수는 시전 상한의 **13%** 수준이다. 묶은 것은 두 가지다:
① 3턴 버프는 시전 행동에서 1턴이 줄어 **덮는 행동이 2회뿐**이고,
② 지불(현재 HP 비율)은 maxHP에 비례해 커지는데 회수(피해의 25%)는 그만큼 안 커진다.

구조적 상한도 계산해 두면: 2행동 × 시전 상한 12% = maxHP 24%인데 지불은 15%다. 즉
**모든 타격이 상한에 닿아도 최대 1.6배**다 — 비율을 아무리 올려도 이 천장을 못 넘는다.

### 2-2. 비용을 깎으면 **나빠진다** (가설 반증)

"수지가 안 맞으니 비용을 깎자"가 자연스러운 첫 수인데, 측정은 반대로 나왔다.
`피의 격노` HP 지불 15% → 5% (지속·비율 불변, 전사 Lv20):

| 지표 | 원본(15%) | 비용 5% |
|---|---|---|
| 1회당 수지 (골렘상) | 0.36× | **1.09×** ✅ |
| 최종 보스 승률 (A / B) | 1.0% / 5.0% | **0.5%** / 5.0% ❌ |
| 골렘상 잔여 HP (A / B) | 95.9% / 95.9% | **71.5%** / 95.9% ❌ |
| 골렘상 전투 길이 (A / B) | 6.2턴 / 6.2턴 | **11.3턴** / 6.2턴 ❌ |
| 전투당 시전 횟수 | 0.00 | **3.00** |

수지는 좋아졌는데 결과는 나빠졌다. 이유는 하나다 — **비용이 싸지면 AI(=사람)가 더 자주 쓰고,
매 시전이 행동 하나를 먹는다.** 3턴 버프는 2행동마다 다시 걸어야 해서 재시전 트레드밀이 생기고,
전투가 6턴에서 11턴으로 늘어나며 그 사이에 맞는 피해가 회복량을 다 잡아먹는다.

`약점 표식`도 같은 모양이었다. 받는 피해를 8% → 20%로 올려도(지속 3턴 유지) 전사 아닌
도적 Lv20 최종 보스에서 **−14.5%p**(시전 4.43회), 15% + 4턴도 **−4.5%p**(시전 2.96회)였다.

### 2-3. 지속만 늘리면 시전이 1회로 떨어지고 전부 뒤집힌다

지속 8턴(비용·효과 불변) 전후, `A`(AI 판단대로) − `B`(미보유):

**피의 격노 (전사)**

| Lv | 상대 | 지표 | 3턴 | 8턴 |
|---|---|---|---|---|
| 25 | 최종 보스 | 승률 A/B | 35.0% / 61.0% (**−26.0p**) | 78.0% / 61.0% (**+17.0p**) |
| 22 | 최종 보스 | 승률 A/B | 6.5% / 15.5% (−9.0p) | 19.0% / 15.5% (+3.5p) |
| 20 | 최종 보스 | 승률 A/B | 1.0% / 5.0% (−4.0p) | 2.0% / 5.0% (−3.0p) |
| 25 | 골렘상 | 잔여 HP A/B | 86.3% / 86.3% (±0) | 96.3% / 86.3% (**+10.0p**) |
| 20 | 암살자상 | 잔여 HP A/B | 35.8% / 72.3% (**−36.5p**) | 65.9% / 72.3% (−6.4p) |
| 20 | 중간 보스 | 잔여 HP A/B | 89.9% / 90.2% (−0.3p) | 95.7% / 90.2% (+5.5p) |
| — | — | 전투당 시전 | 1.90 ~ 2.26 | 0.34 ~ 1.31 |
| — | — | 1회당 수지 | 0.31 ~ 0.48× | **0.79 ~ 1.58×** |

**피의 맹세 (전사 Lv25)**

| 상대 | 지표 | 3턴 | 8턴 |
|---|---|---|---|
| 최종 보스 | 승률 A/B | 25.5% / 61.0% (**−35.5p**) | 65.0% / 61.0% (**+4.0p**) |
| 최종 보스 | 1회당 수지 | 0.45× | **1.41×** |
| 중간 보스 | 잔여 HP A/B | 74.1% / 96.3% (−22.2p) | 84.0% / 96.3% (−12.3p) |
| 골렘상 | 잔여 HP A/B | 86.3% / 86.3% | 79.3% / 86.3% (−7.0p) |

**약점 표식 (도적)**

| Lv | 상대 | 지표 | 3턴 | 8턴 |
|---|---|---|---|---|
| 20 | 최종 보스 | 승률 A/B | 45.5% / 66.5% (**−21.0p**) | 68.0% / 66.5% (**+1.5p**) |
| 22 | 최종 보스 | 승률 A/B | 84.0% / 93.0% (−9.0p) | 92.0% / 93.0% (−1.0p) |
| 25 | 최종 보스 | 승률 A/B | 97.5% / 99.0% (−1.5p) | 98.5% / 99.0% (−0.5p) |
| 20 | 골렘상 | 잔여 HP A/B | 58.8% / 62.2% (−3.4p) | 63.1% / 62.2% (+0.9p) |
| — | — | 전투당 시전 | 1.47 ~ 4.59 | 1.00 ~ 1.77 |

**수지가 전투 길이로 갈린다** — 이게 이번 조정의 핵심 성질이다. 긴 전투(보스·골렘)는 1.1~1.6배로
회수하고, 짧은 전투(암살자상 4~5턴)는 0.79~0.86배로 손해다. "언제 HP를 내고 언제 회수하나"라는
브리프 8-1의 전사 축이 수치로 성립한다 — 잡몹에 거는 건 여전히 낭비다.

### 2-4. 왜 5턴도 99턴도 아니고 8턴인가

99턴(= 전투 내내)도 재어 봤다. 시전이 1.00회로 떨어지는 효과는 같지만, 전투보다 긴 버프는
`피의 맹세`의 "종료 시 지불"을 **영구히 미루는** 형태가 되고, Lv25에서 격노와 맹세를 겹쳐
흡혈 65%를 전투 내내 유지하는 조합(측정: 최종 보스 25.5% → 71.5%)이 생긴다.
8턴은 **일반 전투(4~10턴)에서는 1회 시전, 보스전(13~18턴)에서는 1~2회**가 되는 길이이고,
전투보다 길 수 있다는 점은 3장의 정산으로 닫았다.

## 3. 지속을 늘리면 열리는 구멍 — 「피의 맹세」의 대가를 안 내고 끝난다

`expire_hp_cost`는 `tick_buffs()`가 **만료되는 순간** 받는다. 지속이 8턴이면 전투가 버프보다
먼저 끝나는 일이 흔해지고, 그러면 만료 처리가 한 번도 안 돌아 **대가를 공짜로 넘긴다.**
실측(8턴, 지속만 바꾼 상태): 중간 보스전 지불 **0** · 암살자상 **1**. "고위험 고보상"이 무위험이 된다.

닫은 방식 — `Entity.settle_buff_costs()` + `BattleSession._close_out_battle()`:

- `Entity._pay_expire_cost()`를 뽑아 `tick_buffs()`와 신규 `settle_buff_costs()`가 **같은 코드로** 받게 했다
  (같은 값을 두 번 받지 않고, `hp ≤ 0`이면 아무것도 받지 않는다 — 시체에서 더 받지 않는다).
- `_close_out_battle(msgs)`는 전투가 끝나는 분기에서 ① ATB 이월 ② 남은 버프 대가 정산을 한다.
  승리·도주·패배·전멸 **5곳이 ①의 같은 네 줄을 복사**해 두고 있었고 ②가 그 5곳 전부에 붙어야 해서
  한 함수로 합쳤다 — 한 곳만 빠뜨리면 "그 분기로 끝나면 공짜"가 된다.
- 정산 메시지는 처치·도주·사망 메시지 **뒤에** 붙고, 지불은 피격 장부에 `via="cost"`로 남아
  화면에 숫자로 뜬다(실측 확인).

결과: 중간 보스전 1회당 수지가 0.40배로 **손해가 유지된다** — 짧은 전투에서 맹세를 쓰면 대가를
제대로 낸다는 뜻이다. 최종 보스전은 1.41배로 회수한다.

## 4. 측정용 AI — "곧 끝나는 전투"를 거르게 했다

`PlayerAI._lifesteal_buff_profit()`(reactive 전용)의 기대 회수는 `(turns − 1)`회 행동을 전부
최선의 다타 물리로 때린다고 가정했다. 8턴이 되면 이 가정이 7행동으로 늘어나 **4~5턴에 끝나는
전투에도 무조건 걸어** HP만 버린다(실측: 암살자상 잔여 HP 69.7% → 51.7%).

두 가지를 고쳤다 — 둘 다 `reactive`만 보는 함수이고 **기본 `balanced` 점수는 건드리지 않았다**:

- 덮는 행동 수를 `min(turns − 1, 남은 적 HP ÷ 내 1행동 피해)`로 자른다.
- 회피하면 흡혈도 0이므로 명중률(`1 − (def_luc × 0.4, 상한 25) − dodge_bonus`, `DamageCalc._calc`와 같은 식)을 곱한다.

전사 Lv20 기준 손익: 암살자상(HP 667) **−109** / 골렘상(1382) **+70** / 최종 보스(2484) **+255**.
전투당 시전 횟수가 짧은 전투에서 1.90~2.02 → 0.34~0.70으로 떨어졌고, 암살자상 잔여 HP가
−36.5%p → −6.4%p로 회복됐다.

## 5. 회귀 — 다른 것은 안 움직였다

- **동일 시드 A/B 480전투 IDENTICAL** (최종 RNG 상태 해시까지 일치). `git worktree`로 뜬 HEAD 트리와
  현재 트리에서 같은 드라이버(등급 팩토리 직접 호출 + `balanced`)를 돌려 비교했다 —
  3직업 × 5레벨 × 12몬스터 × (하1/중2/상3) + 보스. 세 스킬을 `balanced`가 고르지 않으므로
  **자동 밸런싱(StatTuner)·튜너 경로·일반 전투 밸런스는 그대로**다.
- 전체 회귀 스위트 46개 파일 전부 통과. `python3 -m compileall` · `create_app()` 정상.
- 구 수치를 기대하던 기존 테스트 2개를 새 계약으로 갱신했다(`test_new_skills.py` ·
  `test_remaining_skills.py`) — 지속값과, "단일 타격이면 손해"처럼 3턴 경제를 전제한 AI 판정.
  둘 다 판정의 **취지**(회수할 시간이 없으면 안 쓴다)는 "곧 끝나는 전투" 케이스로 옮겨 유지했다.
- 지속 **규칙** 자체는 안 건드렸다 — `강화1`(3턴) · `약화1`((3,4))은 그대로이고, 테스트가 고정한다.
  브리프가 경고한 "지속 규칙을 바꾸면 기존 3턴 버프 전부가 같이 움직인다"를 피한 형태다.

## 6. 남은 것 / 판단하지 않은 것

- **`약점 표식`은 AI 손에서 중립이다** (최종 보스 +1.5 / −1.0 / −0.5%p, N=200 표준오차 ±1.8%p 안).
  받는 피해를 8%에서 더 올리면 확실한 이득으로 만들 수 있지만, ① 브리프가 레버 하나를 고르라고 했고
  ② 도적은 이미 구조적으로 가장 강한 직업이라(성장 곡선 문제, BALANCE_PATCH_5 2장) 도적 전용
  스킬을 더 키우는 선택은 따로 판단할 일로 남겼다. 지금 상태로도 **2택 1이 가짜가 아니게** 됐다
  (전에는 표식을 고르면 확실히 손해였다 — 짝인 `둔화2`는 AI가 아예 안 쓰므로 미보유와 동일).
- **`피의 맹세`는 짧은 전투에서 여전히 손해다**(중간 보스 잔여 HP −12.3%p). 정산을 붙인 결과이고
  의도한 성질이다 — 필요하면 AI 게이트에 맹세용 최소 전투 길이 조건을 넣는 쪽이 맞고, 수치는 아니다.
- **유물과 겹친 상태는 안 쟀다.** `짐승의 피`(흡혈 타격 상한 4% → 6%) · `철벽 의지`(흡혈량 2배)는
  이번에 바꾼 회수량을 직접 키운다. 보스 측정 스크립트의 유물 인자(후속 ⑥ 잔여)와 같이 봐야 한다.
- 브리프 아티팩트의 11-1 상태판은 아직 후속 ①·③·⑤를 열린 항목으로 표시한다(③⑤는 5차에서 닫혔다).
  판 갱신은 별도 작업으로 남겼다.
